┌───────────────────────────┐
│   1. UI COMPONENTS        │
│---------------------------│
│ MovieBooking.jsx          │
│ Seat.jsx                  │
└───────────────┬───────────┘
                │ dispatch(action)
                ▼
┌───────────────────────────┐
│   2. ACTIONS (Redux)      │
│---------------------------│
│ loadSeats()               │
│ toggleSeat()              │
│ clearSelectedSeats()      │
└───────────────┬───────────┘
                │ gọi reducers
                ▼
┌───────────────────────────┐
│   3. REDUCERS (Slice)     │
│---------------------------│
│ seatsSlice.js             │
│ - seatList                │
│ - selectedSeats           │
└───────────────┬───────────┘
                │ cập nhật state
                ▼
┌───────────────────────────┐
│   4. REDUX STORE          │
│---------------------------│
│ store.js                  │
│ state = {                 │
│    seats: { ... }         │
│ }                         │
└───────────────┬───────────┘
                │ useSelector()
                ▼
┌───────────────────────────┐
│   5. SELECTORS            │
│---------------------------│
│ seatsSelector.js          │
│ - getSeatList             │
│ - getSelectedSeats        │
└───────────────┬───────────┘
                │ trả dữ liệu
                ▼
┌───────────────────────────┐
│   6. UI RENDER LẠI        │
│---------------------------│
│ Component nhận state mới  │
│ và render lại             │
└───────────────────────────┘
